# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
tfplan

# Local env / generated files
# common.env holds credentials and TF_VAR_* overrides — never commit it
common.env
*.env
.generated/

# Local provider build and generated dev-overrides config (machine-specific paths)
.build/
.terraform-dev.rc

# macOS
.DS_Store
