# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
tfplan

# Local env / generated files
# common.env holds credentials and TF_VAR_* overrides — never commit it
common.env
*.env
.generated/

# macOS
.DS_Store
